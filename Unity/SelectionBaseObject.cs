using UnityEngine;

namespace Core.Plugins.AsgUtils.Unity
{
    /// <summary>
    ///     Add this to a game object to make it select by default when any of the children are selected via the mouse in
    ///     the editor scene view.
    /// </summary>
    [SelectionBase]
    public class SelectionBaseObject : MonoBehaviour
    {
    }
}
